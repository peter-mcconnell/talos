#!/usr/bin/env sh
# help: get a tty inside the toolbox container

TALOS_DIR="${TALOS_DIR:-../../}"

main() {
  # . "${TALOS_DIR}/cmds/docker/run.sh"
  # FLAG_cmd=bash FLAG_tag="$TALOS_IMAGE" main
  bash
}
